# Backend Environment Variables Template for Render
# Copy these to your Render service's Environment settings

# MongoDB Connection (REQUIRED)
# Get this from MongoDB Atlas: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URL=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/expense_tracker_db?retryWrites=true&w=majority

# Database Name (REQUIRED)
DB_NAME=expense_tracker_db

# CORS Origins (REQUIRED)
# For development: use "*"
# For production: use your frontend URL
# Multiple origins: comma-separated list
CORS_ORIGINS=*
# Production example:
# CORS_ORIGINS=https://expense-tracker-frontend.onrender.com

# Google OAuth Credentials (REQUIRED if using Google Sign-In)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Frontend URL (OPTIONAL - for redirects)
# Set this to your frontend Render URL
FRONTEND_URL=https://expense-tracker-frontend.onrender.com

# Python Version (OPTIONAL - Render auto-detects)
PYTHON_VERSION=3.11.0
