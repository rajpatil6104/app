# Frontend Environment Variables Template for Render
# Copy these to your Render static site's Environment settings

# Backend API URL (REQUIRED)
# This should be your backend Render service URL
# IMPORTANT: Do NOT include /api at the end - it's added automatically in the code
REACT_APP_BACKEND_URL=https://expense-tracker-backend.onrender.com

# Node Version (OPTIONAL - Render auto-detects)
NODE_VERSION=18.0.0

# Note: In production, these are set during build time
# Make sure to redeploy frontend if you change REACT_APP_BACKEND_URL
